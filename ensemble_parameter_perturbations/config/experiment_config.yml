var_names_int: ["pr", "rlut", "rsut", "rsutcs", "rlutcs", "lwp", "clvi", "clivi", "dsevi"]
var_names_prof: ["ta", "hus", "clw", "cli", "tke", "wa", "hur", "cl", "arup", "entr"]
# z_levels_shallow: [100, 200, 300, 400, 500, 600, 700, 800, 900, 1000, 1100, 1200, 1300, 1400, 1500, 1600, 1700, 1800, 1900, 2000, 2100, 2200, 2300, 2400, 2500, 2600, 2700, 2800, 2900, 3000, 3100, 3200, 3300, 3400, 3500, 3600, 3700, 3800, 3900, 4000]
reduction_start_time: 345600 # 4 days 
reduction_end_time: 950400 # 11 days - 1 week total integrated time
exp_name: "diagnostic_edmfx"

#################### Data Generation Config ###############################
# All paths below are relative to the ensemble_parameter_perturbations/ project
# root. Every pipeline script resolves them to absolute paths on load via
# `resolve_config_paths!` (src/experiment.jl), so this file can be read from
# any working directory.
prior_path: "config/priors/prior_diagnostic_pi_entr_smooth_entr_detr_coarse_amip_new.toml"
output_dir: "data/output_5_cfsites"
toml_path_name: "parameter"

# for deep vs shallow forcing
forcing_toml_files:
  shallow: "config/tomls/shallow_forcing_relaxation.toml"
  deep: "config/tomls/deep_forcing_relaxation.toml"


z_levels: # [start, step, stop]
  shallow: [100, 100, 4000]
  deep: [100, 100, 4000]
